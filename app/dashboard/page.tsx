import { PageClient } from "./page-client";

export const dynamic = "force-dynamic";

export const metadata = {
  title: "Dashboard - Stride",
};

export default function Dashboard() {
  return <PageClient />;
}
